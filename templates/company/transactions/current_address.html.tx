<p class="text">
    % $form.hidden_field( name => 'address[etag]', value => $etag );
	Current address
	<br />
    % if $care_of_name {
        <% l('c/o') %> <% $care_of_name %>,
    % }
    % if $po_box {
        <% l('PO Box') %> <% $po_box %>,
    % }

	% $old_address;
</p>
